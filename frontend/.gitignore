# Dependencies and build files
node_modules
/frontend/dist
/api/node_modules

# Environment variables (SECRETS)
# This ensures your API keys are NEVER uploaded
.env
.env.local

# System files
.DS_Store